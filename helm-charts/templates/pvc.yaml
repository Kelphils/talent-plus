apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: "{{ .Values.db.name }}-pv-claim"
  namespace: {{ .Values.namespace.name }}
  labels:
    chart: {{ .Chart.Name }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  storageClassName: secrets-store.csi.k8s.io
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi

